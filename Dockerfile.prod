# cz-js-1044 multi-stage Dockerfile with nginx:alpine for React
FROM node:18-alpine AS build
WORKDIR /app
COPY . .
RUN npm ci && NODE_ENV=production npm run build
FROM nginx:alpine
COPY --from=build /app/build /usr/share/nginx/html
